{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4 mb-4">Ville</h1>
{# 
    <h1>Ville</h1> #}

    <table class="table">
        <tbody>
            <tr>
                {# <th>Id</th>
                <td>{{ ville.id }}</td> #}
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.name }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ville_index') }}" class="btn btn-info my-2 mr-8">Revenir à la liste</a>

    <a href="{{ path('app_ville_edit', {'id': ville.id}) }}" class="btn btn-warning my-2 mr-8">Modifier</a>

    {{ include('ville/_delete_form.html.twig') }}
</div>
{% endblock %}
