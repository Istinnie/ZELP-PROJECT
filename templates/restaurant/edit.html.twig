{% extends 'base.html.twig' %}

{% block title %}Modifier le restaurant{% endblock %}

{% block body %}
<div class="container col-4">
    <h1 class="mt-4 mb-4">Restaurant</h1>

    
    {{ include('restaurant/_form.html.twig', {'button_label': 'Modifier'}) }}
    
    <a href="{{ path('app_restaurant_index') }}" class="btn btn-info my-2 mr-8">Revenir à la liste</a>
    
    {{ include('restaurant/_delete_form.html.twig') }}
</div>
{% endblock %}
